name: Update configuration files

on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch:

permissions: {}

jobs:
  call-update-configuration-files:
    name: Update configuration files
    uses: actions/reusable-workflows/.github/workflows/update-config-files.yml@d468c63c53c1184242904d1a3ac74fd1081f36c8 # main
